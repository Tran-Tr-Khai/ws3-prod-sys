export function PlaceholderPage() {
  return (
    <main className="min-h-screen bg-navy p-6 text-slate-800">
      <section className="border border-line bg-panel p-6">
        <p className="mb-2 text-xs font-semibold uppercase tracking-[0.2em] text-industrial">
          WS3 / System Setup
        </p>
        <h1 className="text-2xl font-semibold">Frontend ready</h1>
        <p className="mt-3 max-w-xl text-sm text-slate-600">
          Project foundation is initialized. Production modules will be added in later tasks.
        </p>
      </section>
    </main>
  );
}
